---
description: "[DEPRECATED] Alias de /forge:update-changelog mantido pelo contrato de compatibilidade (C1); remocao prevista na W8.3. Prefira /forge:update-changelog."
---

Este comando foi renomeado para `/forge:update-changelog`.

Execute exatamente as instrucoes de `.claude/commands/forge/update-changelog.md` com os mesmos argumentos recebidos ($ARGUMENTS), sem alterar o comportamento. Ao concluir, informe ao usuario que este alias sera removido e que o nome atual e `/forge:update-changelog`.
