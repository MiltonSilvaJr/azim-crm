<Project Sdk="Microsoft.NET.Sdk">
  <!--
    PartnerManagement.Application.Tests
    Foco: handlers (CreatePartner, UpdatePartner, DeactivatePartner, ReactivatePartner,
          ListPartners, GetPartnerById, GetPartnerEligibility, GetPartnerCommissionView,
          GetPartnerCommissionReport), pipeline behaviors, alerta de duplicado,
          composição da visão de comissão.
    PBTs: PBT-01 (conservação da soma de comissão) e PBT-05 (imutabilidade da comissão
          consolidada) via FsCheck.Xunit (TASK-14).
    Meta de cobertura: ≥ 85% (critério de fechamento da Onda 3).
    Mapeia: Req 1, Req 2, Req 3, Req 4, Req 9, Req 10, design §5, design §13.
  -->
  <PropertyGroup>
    <AssemblyName>PartnerManagement.Application.Tests</AssemblyName>
    <RootNamespace>PartnerManagement.Application.Tests</RootNamespace>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\PartnerManagement.Application\PartnerManagement.Application.csproj" />
    <ProjectReference Include="..\..\src\PartnerManagement.Domain\PartnerManagement.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- NSubstitute: mocks/stubs para IPartnerRepository, IPartnerCommissionReadPort, etc. -->
    <PackageReference Include="NSubstitute" Version="5.3.0" />
  </ItemGroup>
</Project>
