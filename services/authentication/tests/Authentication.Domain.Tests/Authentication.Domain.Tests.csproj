<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Authentication.Domain.Tests
    Foco: imutabilidade/igualdade de AuthContext e Session; transições válidas e
          inválidas de SessionState; Specifications; PBT-01 (sessão sempre escopada
          a um tenant) e PBT-05 unitário (link expirado/consumido nunca concede acesso).
    Gate de CI: Domain.Tests coverage >= 95%.
  -->
  <PropertyGroup>
    <AssemblyName>Authentication.Domain.Tests</AssemblyName>
    <RootNamespace>Authentication.Domain.Tests</RootNamespace>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Authentication.Domain\Authentication.Domain.csproj" />
  </ItemGroup>
</Project>
